using System;
namespace minesweeper_project
{
    public class tiles
    {
        // We want to store our protected coordinates in a list
        // If a user inputs one of these protected coordinates the game will end
        // We can have 2 separate lists :
        // A protected tile list (bombs) => this is generated as soon as the game starts
        // Safe tile list => this gets continually added to as the user inputs more coordinates

        // Potentially could add features where if a user types "Show safe tiles" then we can print the list of cleared tiles
        //
        public tiles()
        {
            int x;
            int y;
        }


    }
}
